#include <stdio.h>

/**
 * main - program that print its name.
 * @argc: argument count
 * @argv: pointer to argument array/ string
 * Return: 0
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
